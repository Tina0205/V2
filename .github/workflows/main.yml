name: Run Python Test
on:
  push:
jobs:
  build:
    name: run python
    runs-on: ubuntu-lastest
    steps:
    - uses: actions/checout@v2 #V2不能更改
    - name: Run python
      run:
        python3 test.py
        
